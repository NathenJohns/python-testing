#Tuples use () whereas a list uses []

coordinates = (4, 5)

'''Tuples cannot be changed, they are immutable,
    whereas lists can be changed'''

# i.e. coordinates[1] = 10

# You can have lists of tuples

coordinates1 = [(4,5), (6,7), (8,9)]

print(coordinates[0])
print(coordinates1[1])
